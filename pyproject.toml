[project]
name = "cortex-xdr-mcp"
version = "0.1.0"
description = "MCP server for Palo Alto Networks Cortex XDR alert investigation"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
